<Page x:Class="WpfAppTestUsickUI.Pages.ViewDataBasePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfAppTestUsickUI.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ViewDataBasePage">

    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <ComboBox Margin="5" FontSize="12" Padding="9"></ComboBox>
            <TextBlock FontSize="20" Grid.Row="1" Text="Путь к файлу" />
            <TextBox Name="PathTextBox"  Grid.Row="2" Height="25" Margin="0 0 95 0" />
            <Button Click="PathClick"  Grid.Row="2" Width="auto" HorizontalAlignment="Right" Margin="0 0 10 0" Content="Выполнить" />
            <DataGrid Margin="5" MinHeight="200" Grid.Row="3"></DataGrid>
            <Button  Margin="5" FontSize="14" MinHeight="30" Grid.Row="4" Content="Назад" Click="BackClick"/>
        </Grid>
    </Grid>
</Page>
